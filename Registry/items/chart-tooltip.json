{
  "$schema": "../schema.json",
  "schemaVersion": 1,
  "version": "0.1.0",
  "name": "chart-tooltip",
  "kind": "recipe",
  "description": "chartXSelection + RuleMark annotation.",
  "usage": "@State private var selectedMonth: String?\n\nvar selected: MonthlyBalance? {\n    balances.first { $0.month == selectedMonth }\n}\n\nChart {\n    ForEach(balances) { row in\n        BarMark(\n            x: .value(\"Month\", row.month),\n            y: .value(\"Balance\", row.amount)\n        )\n        .foregroundStyle(TintShapeStyle())\n    }\n    if let selected {\n        RuleMark(x: .value(\"Month\", selected.month))\n            .foregroundStyle(.secondary)\n            .annotation(position: .top) {\n                Text(selected.amount, format: .currency(code: \"KWD\"))\n                    .font(.footnote.monospacedDigit())\n                    .padding(6)\n                    .registrySurface()\n            }\n    }\n}\n.chartXSelection(value: $selectedMonth)\n.registryChart()",
  "docs": "`chartXSelection` binds mark's x type; tap/drag native. Adds `RuleMark` + top annotation on registry surface; only `.registryChart()` themes. Single-series needs explicit accent, else default color. Keep annotation short; type MUST match mark's x, or nothing selects.",
  "files": [],
  "registryDependencies": [],
  "packageDependencies": [],
  "platforms": [
    {
      "name": "iOS",
      "minimumVersion": "26.0"
    }
  ],
  "tags": [
    "chart-tooltip",
    "chart",
    "selection",
    "annotation",
    "guidance"
  ],
  "aliases": [
    "chart-selection",
    "data-tooltip"
  ],
  "accessibility": [
    "Marks expose to VoiceOver via audio graph, value text.",
    "Selected value: visible text, not color alone."
  ],
  "preview": {
    "screenshots": [
      "docs/images/items/chart-tooltip-light.png",
      "docs/images/items/chart-tooltip-dark.png"
    ]
  }
}

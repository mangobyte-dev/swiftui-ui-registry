{
  "$schema": "../schema.json",
  "schemaVersion": 1,
  "version": "0.1.1",
  "name": "select",
  "kind": "component",
  "description": "Adds semantic field chrome to a native menu-style Picker without replacing its presentation or selection behavior.",
  "files": [{ "source": "sources/components/RegistrySelectModifier.swift", "target": "RegistrySelectModifier.swift" }],
  "registryDependencies": [],
  "packageDependencies": [{ "package": "SwiftUIRegistry", "product": "SwiftUIRegistryFoundations", "requirement": "0.x" }],
  "platforms": [{ "name": "iOS", "minimumVersion": "18.0" }],
  "tags": ["select", "picker", "menu", "form", "modifier"],
  "accessibility": [
    "Retains native Picker menu, selection, and dismissal behavior.",
    "Keeps option labels and selection caller-owned.",
    "Provides a 44-point minimum control height and visible disabled treatment.",
    "Inherits Dynamic Type and layout direction."
  ],
  "preview": { "source": "sources/components/RegistrySelectModifier.swift", "name": "Select" }
}

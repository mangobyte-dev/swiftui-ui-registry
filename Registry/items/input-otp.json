{
  "$schema": "../schema.json",
  "schemaVersion": 1,
  "version": "0.1.0",
  "name": "input-otp",
  "kind": "recipe",
  "description": "One-time-code TextField, not a boxed control.",
  "usage": "@State private var code = \"\"\n\nTextField(\"One-time code\", text: $code)\n    .textContentType(.oneTimeCode)\n    .keyboardType(.numberPad)\n    .textFieldStyle(.registryInput)\n    .font(.title2.monospacedDigit())\n    .accessibilityLabel(\"Verification code\")\n    .onChange(of: code) { _, newValue in\n        code = String(newValue.filter(\\.isNumber).prefix(6))\n    }\n\nButton(\"Verify\") { }\n    .buttonStyle(.registry)\n    .disabled(code.count < 6)",
  "docs": "`TextField` + `.textContentType(.oneTimeCode)`: offers Messages/Mail code above keyboard; `.keyboardType(.numberPad)`: numeric. Six-box imitates, loses autofill. `onChange` caps 6 digits. No type: no offer.",
  "files": [],
  "registryDependencies": [],
  "packageDependencies": [],
  "platforms": [
    {
      "name": "iOS",
      "minimumVersion": "26.0"
    }
  ],
  "tags": [
    "input-otp",
    "otp",
    "verification-code",
    "one-time-code",
    "guidance"
  ],
  "aliases": [
    "otp",
    "pin-code"
  ],
  "accessibility": [
    "Needs explicit label; title shows only as placeholder.",
    "Monospaced-digit font: legible at large Dynamic Type."
  ],
  "preview": {
    "screenshots": [
      "docs/images/items/input-otp-light.png",
      "docs/images/items/input-otp-dark.png"
    ]
  }
}

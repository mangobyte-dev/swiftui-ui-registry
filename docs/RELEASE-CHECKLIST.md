# Release checklist

The contract for a public beta of this repository, written 2026-09-08 from how shadcn-ui/ui and the pointfreeco packages present a release, then made concrete for this repo. A line closes only with evidence: a command's output, a file path with a line, or a test log with executed counts. A line that cannot close says why

What the reference repositories do, read on 2026-09-08 from the GitHub API listing of each repository root and its releases:

- shadcn-ui/ui keeps `README.md`, `CONTRIBUTING.md`, `LICENSE.md`, `SECURITY.md`, `RELEASING.md`, and `.github/` with issue templates, Dependabot, and workflows at the root; release notes are changesets grouped as Minor Changes and Patch Changes with one line per change and its pull request. `CONTRIBUTING.md` opens with the repository layout as a table
- pointfreeco/swift-dependencies, swift-sharing, and swift-composable-architecture keep `README.md`, `LICENSE`, `Makefile`, `Package.swift`, `.spi.yml`, `.editorconfig`, `Sources/`, `Tests/`, `Examples/` (sharing, TCA), and `.github/` with `CODE_OF_CONDUCT.md`, `ISSUE_TEMPLATE/`, and `workflows/`; no `CHANGELOG` file, the release notes live on the tag as What's Changed with Added, Fixed, and Infrastructure bullets and a Full Changelog compare link; tags are bare semantic versions with no `v` prefix. Their README runs intro, Learn More, Overview, Quick start, Examples, Documentation, Installation, Community, License

This repository already tags bare versions (`0.1.0`, `0.2.0`), keeps its release notes on the GitHub release, and builds the universal binary from `.github/workflows/release.yml`. The beta adds a `CHANGELOG.md` at the root so the notes exist before the tag and survive outside GitHub

## 1. Repository shape

- [x] 1.1 The root holds only what an adopter needs: `README.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `LICENSE`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `AGENTS.md`, `CLAUDE.md`, `Package.swift`, `Package.resolved`, `Makefile`, the dot files, `Distribution/`, `Examples/`, `Registry/`, `Scripts/`, `Skills/`, `Sources/`, `Tests/`, `Website/`, `docs/`. Evidence: `git ls-files` at depth 1 after ef7e727 lists exactly the allowed set (documenter's report, 22:20); the untracked tooling directories are gitignored
- [x] 1.2 Planning, research, run logs, and notes to self are moved out of git into `~/Projects/swiftui-cn-local/` with a one-line index there, each judged by reading it: `HANDOFF.md`, `PRODUCT.md`, `GENERAL_DIRECTION_REVIEW.md`, `STAGE_ONE_VALIDATION.md`, `docs/component-roadmap.md`, `docs/research.md`, `docs/research-stage-7.md`, `docs/clean-room-trial.md`, `docs/cli-migration.md`, `tasks/`, and the untracked `tmp/` and `swarmforge/`. `docs/mango.md` stays as the design-system template if its research citations can be rewritten as plain statements. Evidence: commit ef7e727 removed the ten tracked files (2011 lines) and `~/Projects/swiftui-cn-local/INDEX.md` lists each with its date; `tmp/` and `swarmforge/` moved there untracked
- [x] 1.3 Every link, README section, skill, test, and generator that pointed at a moved file is updated; `git grep` for each moved name returns nothing outside `docs/catalog/` and `Website/content/`. Evidence: commits 3eb4817, b0a13a9, e6fdf93 fixed every kept file; the last two hits, `Registry/items/sheet.json` and `SwiftUIRegistryShowcaseApp.swift`, were reworded by the orchestrator at 22:08 with the generators rerun (`git grep component-roadmap` outside generated paths and the checklist returns only the contract test's comment saying the file left)
- [x] 1.4 `AGENTS.md` describes the shipped repository: its document map names only files that exist, state lives in `CHANGELOG.md` (shipped and known limitations) rather than a roadmap, and the verification list runs as written. Evidence: commit e6fdf93: the document map names CHANGELOG.md as the only home of state and the archives outside the repository; the stale CI sentence and floors are gone
- [x] 1.5 Every kept markdown file is read in full and corrected: stale counts, dead paths, wrong commands, plans described as behavior. No em dashes in prose anywhere in the tree (`git grep -n "—" -- '*.md' '*.swift' '*.ts' '*.tsx'` is empty outside quoted third-party text). Evidence: commits 3eb4817, b0a13a9, 1be71d7: every kept markdown read in full (documenter's report); `git grep -n "—" -- '*.md'` returns only `Website/AGENTS.md:4`, inside the block create-next-app vendors (`BEGIN/END:nextjs-agent-rules`), and this checklist's own pattern
- [x] 1.6 `CONTRIBUTING.md` opens with the repository layout and the verification list, as the reference repositories do; `SECURITY.md` names the supported versions by tag; `CODE_OF_CONDUCT.md` stands. Evidence: commit 1be71d7: CONTRIBUTING opens with the layout table and points at the verification list; SECURITY names the newest tag and `main`
- [x] 1.7 `.github/` keeps the issue templates, the pull request template, Dependabot, CODEOWNERS, and the three workflows, and each workflow's steps still match the repository after the moves. Evidence: the documenter read the three workflows and the templates and found no moved-file reference (report, 22:20); the orchestrator widened `ci.yml`'s drift check to the item-tokens generator and its output, which the AGENTS.md list already covered

## 2. The Stage 9 surface, polished

Driven on the iOS 27 simulators (iPhone 17 and iPad Pro 13-inch) in the Showcase and in both host apps. Every defect gets a failing test first where a test can hold it, then the fix; the tool's chrome stays fixed (system values, never the tuned theme)

- [x] 2.1 Open, select, tune: the Tune button opens the card, Select picks the innermost item, a knob moves the item live, the card's chrome does not move. Evidence: Showcase on the iPhone 17: Tune opened the card, Select picked the Sign in button (`tuning.scope.item` = button, 7 tokens), the ring drew in the tool's blue while the card held still (screenshots select-1.png and the argent captures at 22:53); seeFood check 1 passed on the iPhone 17 Pro QA with a padding knob moving the live card (workflow report)
- [x] 2.2 Drag the card off every edge and back: leading, trailing, bottom, top; the grab strip stays reachable; the card never leaves the safe area past its strip. Evidence: driven with argent drags at 22:56 to 23:00: leading (strip and chevron stayed), trailing, bottom (only the strip above the tab bar), and the top stop at the status bar's edge after the safe-area fix (1dd311f); `PanelGeometryTests` hold the clamp; seeFood check 2 passed
- [x] 2.3 Resize to the minimum and the maximum; the content stays usable at the minimum and the grip stays reachable at the maximum. Evidence: the grip shrank the card to 300 by 260 with the toolbar still usable (the title was dropped so the four controls fit) and grew it to the full width with the grip on the home-indicator line (screenshots at 23:02 and 23:03); `Resizing stops at the far edges` in `PanelGeometryTests`; seeFood check 3 passed
- [x] 2.4 Rotate with the card up; the frame is clamped into the new area and remembered per size class. Evidence: `testFloatingPanelStaysReachableAfterRotation` passed on the iPad Pro 13-inch (ipad-tests-1.log), seeFood check 4 passed on the iPhone (the card re-clamped into the landscape area); the Showcase is portrait-only on iPhone, so the test skips there
- [x] 2.5 Collapse and reopen; the frame and the selection survive. Evidence: `testCollapsingAndReopeningThePanelKeepsTheSelection` passed on the iPad (ipad-tests-1.log) after the collapse frame fix; seeFood check 5 passed with the frame and the selection kept
- [x] 2.6 A sheet and a full-screen cover under the panel: items inside them select, the card stays above, the sheet's own touches work. Evidence: on the iPad Pro 13-inch the dialog recipe's sheet opened under the column, the card stayed above it, its Name field took the keyboard with the panel up once the app kept the key window (screenshot 1788896326170), and the panel's own Import editor took it back (1788896365020); seeFood's first-run Welcome sheet showed the Tune button above it (kutayib report). Selecting a tagged item inside a sheet was not driven: the Showcase's sheets hold no tagged item and seeFood's check 6 was not reached [unverified]; the reporter travels through the environment, which a sheet inherits, and that is the design reason it works
- [x] 2.7 The keyboard in a panel text field: the card keeps its place, the field is reachable, the keyboard dismisses cleanly. Evidence: the Email field of the auth-form block took the keyboard with the card up, and the panel's Import editor took it back (screenshots at 23:19 and 22:14); the full suite's import test types into that editor
- [x] 2.8 Select on nested items: the chain lists every item under the pick, innermost first; a pick with nothing under it clears the selection without a crash and leaves Select disarmed. Evidence: the Sign in button inside the card inside the form resolved to button with the chain button, card, auth-form (On this screen listed all three); `testAPickOnNothingClearsTheSelectionAndDisarmsSelect` passed on the iPhone and the iPad; seeFood check 8 passed with the innermost-first chain in its Picked section
- [x] 2.9 Reduce Motion: no animated card movement or selection ring animation beyond what the setting allows. Evidence: Reduce Motion on (`defaults write com.apple.Accessibility ReduceMotionEnabled true`): the card landed in place within 60 ms of the tap with no crash (23:11); `ToolChrome.animation(reduceMotion:)` returns nil, held by `The tool moves without animation under Reduce Motion`; the two remaining `.snappy` toggle bindings now go through the gate (1dd311f); seeFood check 9 passed
- [x] 2.10 Dynamic Type at accessibility sizes: the panel's rows, the drag bar, and the grip stay usable; the minimum size grows with the text. Evidence: accessibility-extra-large: the rows, the drag bar, and the grip stayed usable and the toolbar overflowed natively (ax-size-1.png); the minimum grows to 360 points and re-clamps on the change (1dd311f, `The minimum grows at an accessibility text size`); seeFood check 10 passed
- [x] 2.11 Dark and light: the card, the ring, the outlines, and the guides read on both. Evidence: dark: the card, the dashed ring on both buttons, and the labels read on the dark ground (23:05 capture); light throughout the rest of the drive; seeFood verified dark (its app pins the scheme)
- [x] 2.12 Right to left: the card's default place, the snap edge, the grab strip, and the outlines mirror correctly. Evidence: launched with `-right-to-left`: the outlines and the pick landed on the mirrored items (rtl-outlines.png); the overlay positions every layer in window points, so a system-language RTL app gets the same result; neither host app renders RTL without an Arabic localization, so a system-RTL run is unverified [unverified]
- [x] 2.13 VoiceOver: every control in the window has a label; the Tune button, the drag bar, the collapse, the grip, the Select capture, the On this screen rows. Evidence: `testEveryPanelControlSpeaksItsLabel` passed on the iPhone and the iPad; argent's describe lists a label on every tool control in the Showcase and in seeFood (check 13)
- [x] 2.14 A host with no page for a picked item: the panel stays on its form, no push, no empty page. Evidence: a pick with no host page leaves the panel on its form (`TuningPanel` pushes only when `hostPage` returns a view); exercised in the Showcase, which registers no pages, on every pick tonight; seeFood check 14 was not reached [unverified in a host with pages]
- [x] 2.15 A host document that fails to decode: the store falls back to the shipped document, logs once, and never crashes; the file is not clobbered until a change is made. Evidence: garbage written into the Showcase's registry-tokens.json with the app stopped: the relaunch and the panel showed the defaults and the file's SHA was unchanged after opening the panel (23:13); seeFood check 15 passed on its host document; both held by the two decode tests in `SurfaceBoundaryTests`
- [x] 2.16 A remembered card frame after a size-class change: the compact and regular frames are separate and each is clamped when restored. Evidence: the compact and regular frames live under separate keys and each restores clamped (`PanelGeometryTests`, the `onChange(of: isRegular)` restore in `FloatingPanel`); seeFood check 16 saw the regular column on the iPad; a live Split View change was not driven [unverified at runtime]
- [x] 2.17 Memory when the panel opens and closes 50 times: no growth beyond the first open (measured with the simulator's memory report or an XCTest metric). Evidence: `testOpeningAndClosingThePanelFiftyTimesHoldsMemory` on the iPad: Memory Physical deltas per ten cycles 9667, -377, 0, 16, -16 kB (ipad-tests-1.xcresult), so only the first open allocates
- [x] 2.18 The Showcase UI suite passes on the iPhone 17 pin with executed counts read from the log; the two stale references are the only known failures or are recaptured with a GOLDEN-CHANGE note by the owner. Evidence: the full iPhone run at 22:38 (iphone-full-suite.log): `Executed 27 tests, with 2 tests skipped and 10 failures`; the two skips are the iPad-only tests, three failures are visual references (`auth-light` 2.70 percent and `nutrition-light` 1.54 percent, the owner's stale references; `activity-light` 4.55 percent from a text size left at medium during the drive), and the seven panel failures came from a card left small at the bottom before the layout reset landed. Reruns with the final build: 10 tests with 1 failure at 23:07 (iphone-rerun.log), then the scroll rule fixed and 4 tests with 0 failures at 23:43 (iphone-rerun-4.log); the activity reference passed in the rerun. So every test passes on the final build except the two stale references
- [x] 2.19 seeFood's Design mode runs on the registry product with no app-side workaround; any one-line adoption edit is noted. Evidence: seeFood built against tonight's registry through a path override and every check it reached passed (1 to 5, 8 to 11, 13, 15, 16, 18); no app edit beyond the override; the override is restored before morning and the pin moves to 0.3.0 as the owner's step
- [x] 2.20 kutayib builds against the registry and its installed items render; any one-line adoption edit is noted. Evidence: kutayib built with the override plus the adoption (`import SwiftUIRegistryDesignSurface`, `.designSurface()`); its six items render (check 18) and every tool control is labeled (check 13); Select finds nothing because its items predate the tag (CHANGELOG known limitation)

## 3. Edge cases at the product boundary

Every public symbol in `SwiftUIRegistryFoundations` and `SwiftUIRegistryDesignSurface` read, asking what a host can pass that breaks it

- [x] 3.1 `ItemSelection.chain` and `pick` with an empty frame list, duplicate names, zero-area frames, and a point outside every frame. Evidence: `SurfaceBoundaryTests` (empty list, point outside, duplicate names, zero-area frame, equal-area tie order), 43 Showcase package tests passed in showcase-package-tests-3.log; ties now resolve by name in `ItemSelection.chain` (bd4b385)
- [x] 3.2 `designSurface` applied twice in one tree, or with `knobs` registered twice for one item: the later registration wins or is rejected, never a crash. Evidence: `KnobStore.register` merges with the later registration winning, held by `Registering knobs twice for one item keeps the later registration` (bd4b385); a second `designSurface` in one tree reuses the one window (`install()` guards on the existing window) and the last `onAppear` writes the host hooks
- [x] 3.3 `itemTitle` returning an empty string: the row and the ring fall back to the name. Evidence: `DesignSurfaceState.title` falls back to the name for an empty host title, held by `A host title that is empty falls back to the item's name` (1dd311f); the ring reads the same title through `selectedTitle`
- [x] 3.4 `page` returning a view for an item that is not on screen, and a page closure that is expensive: called once per pick, never per frame. Evidence: `TuningPanel` calls `hostPage` only when the chain changes (once per pick, for each name in the chain until one has a page) and once more to build the pushed destination; frames never reach it (`TuningPanel.swift`, the `onChange(of: chain)` block). A page for an item that is not on screen simply pushes; nothing reads frames there
- [x] 3.5 `registryScreen` names that nest, repeat, or are empty; `registryItem` with an empty name. Evidence: `registryItem("")` and `registryScreen("")` report nothing (`RegistryItem.swift`, bd4b385); nested and repeated screen names stack and leave by their last index (`DesignSurfaceState.screenLeft`)
- [x] 3.6 A token file written by a newer version, an empty file, or a file with unknown keys: decode falls back and is logged, the shipped document applies. Evidence: `TokenStore` keeps the shipped document, logs once through `SurfaceLog`, and leaves the file untouched until a change; held by the two decode tests in `SurfaceBoundaryTests`, which read the real file system through `withDependencies` (bd4b385). Unknown keys decode fine through synthesized Codable
- [x] 3.7 `ItemKnob` with an inverted range, a zero step, or a shipped value outside the range: clamped or rejected at registration with a clear message. Evidence: `ItemKnob.init` makes a non-positive step 1 and clamps a shipped value into the range, logged; an inverted `ClosedRange` traps in the host's own code before it reaches the surface. Held by `A knob with a zero step or a shipped value off its range is made usable` (bd4b385)
- [x] 3.8 `TokenDocument.pages` empty, a page with no knobs, a choice knob whose current value is not among its options. Evidence: a document with no pages shows only its reset; a page with no knobs is an empty section; a choice knob whose value is not among its options adds the value to the picker (`TokenPageView`, bd4b385)
- [x] 3.9 Package tests hold each boundary case that can be held without UIKit; Showcase package tests hold the rest. Evidence: the boundary cases live in the Showcase package tests because the product compiles empty on macOS: `SurfaceBoundaryTests` (11 tests) and `PanelGeometryTests` (8 tests); the root package holds the foundations theme tests (8) and RegistryKit (71)

## 4. The tool and the installer

- [x] 4.1 `search`, `describe`, `info`, `install --plan`, `install`, `install --diff`, `install --update`, and `preset` run against a scratch package with expected output. Evidence: toolsmith's report (22:25): search, describe, install --plan, install, info, --diff (0 then 1 after an edit), a refused reinstall (exit 2), --update, the five preset commands, and `mcp` tools/list (7 tools) all ran against the scratch package with the expected output
- [x] 4.2 The same against seeFood's and kutayib's package trees: receipts round-trip through `info`, `--diff` reports the owned edits, `--update` without `--force` never overwrites a customized owned item. Evidence: toolsmith's report: `info` round-trips both receipts (seeFood 15 items with bubble, button, card, empty owned; kutayib 6 with button, input owned); `--update` on scratch copies left seeFood's sources untouched and on kutayib merged input cleanly and stopped on button with exit 2, a `.merge` artifact, and the owned file byte-identical
- [x] 4.3 The skills' `.swiftinterface` references are regenerated from the built products and match the public API. Evidence: commit 8af740e regenerated the foundations `.swiftinterface` after bd4b385 removed the anchor preference
- [x] 4.4 The root package tests pass with the executed count read from the log. Evidence: toolsmith's `swift test`: RegistryKitTests 71 passed, SwiftUIRegistryFoundationsTests 8 passed, 0 issues (report, 22:25)
- [x] 4.5 The Showcase package tests pass with the executed count read from the log. Evidence: `Test run with 44 tests in 5 suites passed` on the iPad (ipad-tests-1.log, ipad-tests-2.log) and on the iPhone (iphone-full-suite.log)
- [x] 4.6 The Showcase UI suite passes on the iPhone 17 pin with the executed count read from the log (see 2.18). Evidence: see 2.18: 27 executed in the full run, then 10 and 4 in the reruns on the final build, the two stale references the only remaining failures; the iPad ran 7 and 9 panel tests with 0 failures (ipad-tests-1.log, ipad-tests-2.log)
- [x] 4.7 The generated outputs are byte-identical after the four generators (`git diff --exit-code` on the generated paths). Evidence: toolsmith's report: `git diff --exit-code` on the four generated path sets after the generators, exit 0 (5fa2ada)
- [x] 4.8 `make format-check` passes. Evidence: `make format-check` exit 0 (toolsmith's report)
- [x] 4.9 The website typechecks and builds. Evidence: `npm ci`, `npm run typecheck`, `npm run build` in Website exit 0 (toolsmith's report)

## 5. Release artifacts

- [x] 5.1 `CHANGELOG.md` at the root with the beta entry in adopter language: the design surface as a window over a host app, `TokenDocument`, per-item knobs, the host hooks, `ItemSelection.chain`, fixed tool chrome, the off-edge card, and the known limitations. Evidence: commits cf15db2 (draft) and 91814b2 (finished): Added, Changed, Fixed, and Known limitations for 0.3.0 in adopter language, the 0.2.0 and 0.1.0 history from the release notes
- [x] 5.2 `README.md` reflects the current API and opens with a five-minute quickstart that works when followed literally. Evidence: commit 91814b2: the quickstart's snippets are quoted from `describe button` and `install --plan` output at 23:08, the versions read 0.3.0, the design surface section documents the three forms and the host overload against `DesignSurface.swift`
- [x] 5.3 The version constant in `Sources/RegistryKit/ReleaseSnapshot.swift`, the Homebrew formula template, every installable item's foundations floor, and the requirement the installer prints agree on the beta version. Evidence: commit 5fa2ada: the version constant, the formula, the 48 installable item floors, and the printed requirement (`from 0.3.0 up to the next minor version`) agree; the update notice no longer advertises an older tap tag
- [x] 5.4 The tag and push commands are prepared in the morning report; nothing is pushed or tagged tonight. Evidence: the commands are in the morning report; `git status` shows no push and `git tag` still lists only 0.1.0 and 0.2.0 at 23:25
- [x] 5.5 Each commit tonight has a subject, a body saying what changed and why, and one `By <role>.` line, nothing else. Evidence: `git log --format=%B ec546e9..HEAD` audited at 23:32: every commit has a subject, a body, and exactly one `By <role>.` line, no other trailer

## Beta version

`0.3.0`. Foundations gained public API after `0.2.0` (`registryItem`, `registryScreen`, `registryKnob`, the reporter) and the design surface became a second product; under 1.0 that is a minor bump, and every installable item applies `registryItem`, so the item floor is the tag itself

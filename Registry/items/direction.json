{
  "$schema": "../schema.json",
  "schemaVersion": 1,
  "version": "0.1.0",
  "name": "direction",
  "kind": "component",
  "description": "Documents and previews native layoutDirection behavior and semantic leading/trailing layout without a replacement API.",
  "files": [{ "source": "sources/components/RegistryDirectionExamples.swift", "target": "RegistryDirectionExamples.swift" }],
  "registryDependencies": [],
  "packageDependencies": [],
  "platforms": [{ "name": "iOS", "minimumVersion": "18.0" }],
  "tags": ["direction", "layout-direction", "rtl", "localization", "guidance"],
  "accessibility": [
    "Uses semantic reading order and leading/trailing alignment.",
    "Treats illustrative symbols as decorative.",
    "Relies on SwiftUI's environment propagation instead of duplicating locale state.",
    "Demonstrates both left-to-right and right-to-left layouts."
  ],
  "preview": { "source": "sources/components/RegistryDirectionExamples.swift", "name": "Direction Left to Right" }
}

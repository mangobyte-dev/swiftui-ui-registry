{
  "$schema": "../schema.json",
  "schemaVersion": 1,
  "version": "0.1.1",
  "name": "dialog",
  "kind": "recipe",
  "description": "`.sheet`: system owns detents, drag-dismissal, focus.",
  "usage": "@State private var isEditing = false\n\nButton(\"Edit profile\") { isEditing = true }\n    .sheet(isPresented: $isEditing) {\n        NavigationStack {\n            ProfileEditor()\n                .navigationTitle(\"Edit profile\")\n                .toolbar {\n                    ToolbarItem(placement: .confirmationAction) {\n                        Button(\"Done\") { isEditing = false }\n                    }\n                }\n        }\n    }\n    .registryItem(\"dialog\")",
  "docs": "`.sheet` + `NavigationStack`: edit/detail → chrome gets title, confirmation. Grabber, dismiss, keyboard, Liquid Glass: system. No wrapper.",
  "files": [],
  "registryDependencies": [],
  "packageDependencies": [
    {
      "package": "SwiftUIRegistry",
      "product": "SwiftUIRegistryFoundations",
      "requirement": "0.x",
      "sourceURL": "https://github.com/mangobyte-dev/swiftui-ui-registry.git",
      "swiftPM": {
        "kind": "upToNextMinor",
        "minimumVersion": "0.3.0"
      }
    }
  ],
  "platforms": [
    {
      "name": "iOS",
      "minimumVersion": "26.0"
    }
  ],
  "tags": [
    "dialog",
    "sheet",
    "modal",
    "presentation",
    "guidance"
  ],
  "aliases": [
    "modal",
    "sheet"
  ],
  "accessibility": [
    "Sheets: modal VoiceOver, swipe-escape dismiss.",
    "Toolbar: system semantics, Done-Cancel.",
    "Sheet content: registry items, any-screen."
  ],
  "preview": {
    "screenshots": [
      "docs/images/items/dialog-light.png",
      "docs/images/items/dialog-dark.png"
    ]
  }
}

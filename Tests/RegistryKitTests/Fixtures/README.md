# Migration oracle fixtures

- `schema.json`, `preset_vectors.json`: byte copies of canonical `Registry/schema.json`, shared codec `Registry/preset_vectors.json`, also reproduced by website TS, Showcase Swift; `fixturesMatchCanonicalContracts` rejects drift; `preset-vectors-check.ts` runs `Website/lib/preset.ts` under `node --experimental-strip-types` (`websiteCodecReproducesEveryVector`)
- `validation-cases.json`: 45 distinct calls, unchanged `test_validation.py` (`eab60f4`): name, registry files, scope, exact ordered-issues; Swift loads files into in-memory filesystem, parses real validate command, compares exit status and both output streams; malformed fixture can't pass by throwing unrelated error
- `diff-cases.json`: 106 `difflib.unified_diff` results, three context lines: insertions, deletions, replacements, repeats, empty files, missing final newlines, CRLF, other splitlines-edges, SequenceMatcher autojunk threshold; random, fixed seed 42
- Inline install/merge/conflict snapshots (`Commands.swift`, `InstallerSafetyTests.swift`) from `Scripts/install.py`: every directory, owned-source/receipt/base bytes, conflict artifact; `PresetCommandSnapshots.swift` from `Scripts/preset.py`
- `Scripts/check_swift_parity.py`: subprocess proof, Phase A/B, removed with Python path in Phase C (`796b399`, 1,184 comparisons): cloned registry into temporary clone, compared stdout/stderr/exit, complete unnormalized trees; interop: both destinations started Python receipt then Swift receipt, updated with both tools
- `RegistryContractTests.swift`, `MCPContractTests.swift`, `PresetContractTests.swift`: assertions ported from former Python suites reading real registry: native seams, value gate, token-consumer contract, block-resolution order, receipt provenance; plan/diff over real items, search ranking/aliases, MCP tools over real catalog, preset spellings
- `MCPTests.swift`: seven successful tool responses as exact inline wire snapshots, Python-server's `example` fixture; protocol and refusal snapshots: parse errors, notification-silence, unknown methods/tools/items, bad arguments; persistent-metadata reload and home expansion: injected effects
- `GeneratorTests.swift`: snapshots three real generator commands, checks orphan-cleanup/index-name refusal, regenerates full repository into in-memory destination, diffs byte-for-byte: every catalog page, both manifests, site-JSON, images; three Python generator-module publishing tests become usage/ordering/recipe/source/screenshot assertions

{
  "$schema": "../schema.json",
  "schemaVersion": 1,
  "version": "0.1.0",
  "name": "sheet",
  "kind": "recipe",
  "description": "Native guidance for a side sheet with inspector on iPad that adapts to a sheet on iPhone.",
  "usage": "@State private var isShowingDetails = false\n\nNavigationStack {\n    List {\n        LabeledContent(\"Merchant\", value: \"Mishmash Bakery\")\n        LabeledContent(\"Amount\", value: \"KWD 8.750\")\n    }\n    .navigationTitle(\"Transaction\")\n    .toolbar {\n        ToolbarItem(placement: .topBarTrailing) {\n            Button(\"Details\", systemImage: \"sidebar.trailing\") {\n                isShowingDetails.toggle()\n            }\n            .accessibilityLabel(\"Toggle details\")\n        }\n    }\n    .inspector(isPresented: $isShowingDetails) {\n        List {\n            LabeledContent(\"Category\", value: \"Dining\")\n            LabeledContent(\"Card\", value: \"Visa 4321\")\n            LabeledContent(\"Status\", value: \"Cleared\")\n        }\n        .inspectorColumnWidth(min: 240, ideal: 280, max: 360)\n    }\n}",
  "docs": "`.inspector(isPresented:)` shows a trailing column on iPadOS and a sheet on iPhone, sized with `.inspectorColumnWidth(min:ideal:max:)`. The `dialog` and `drawer` recipes cover the plain sheet; this recipe adds the inspector form. The registry measured on iOS 27 that an inspector attached to a tab's navigation stack stopped text-field focus from advancing, so attach it above a stack or keep to `.sheet` when a tab holds text fields. Watch that caveat before placing an inspector inside a tabbed flow.",
  "files": [],
  "registryDependencies": [],
  "packageDependencies": [],
  "platforms": [
    {
      "name": "iOS",
      "minimumVersion": "26.0"
    }
  ],
  "tags": [
    "sheet",
    "inspector",
    "side-sheet",
    "detail-panel",
    "guidance"
  ],
  "aliases": [
    "inspector",
    "side-panel"
  ],
  "accessibility": [
    "The inspector toggle is a labeled button, and the panel content is reachable by VoiceOver.",
    "On iPhone the inspector adapts to a sheet with the system dismiss affordance."
  ],
  "preview": {
    "screenshots": [
      "docs/images/items/sheet-light.png",
      "docs/images/items/sheet-dark.png"
    ]
  }
}

# Release checklist

Public beta contract, 2026-09-08, from shadcn-ui/ui and pointfreeco practice. A line closes only with evidence: command output, file+line, or test log with counts; else states why.

Reference repos, read 2026-09-08 (GitHub API root listing, releases):

- shadcn-ui/ui root: `README.md`, `CONTRIBUTING.md`, `LICENSE.md`, `SECURITY.md`, `RELEASING.md`, `.github/` (issue templates, Dependabot, workflows); notes: changesets, Minor/Patch Changes, one line per change/PR; `CONTRIBUTING.md` opens with layout table
- pointfreeco (swift-dependencies, swift-sharing, swift-composable-architecture): `README.md`, `LICENSE`, `Makefile`, `Package.swift`, `.spi.yml`, `.editorconfig`, `Sources/`, `Tests/`, `Examples/` (sharing, TCA); `.github/` adds `CODE_OF_CONDUCT.md`, `ISSUE_TEMPLATE/`, `workflows/`; no `CHANGELOG`; notes: tag as What's Changed (Added/Fixed/Infrastructure, Full Changelog link); bare semver tags, no `v`; README order: intro, Learn More, Overview, Quick start, Examples, Documentation, Installation, Community, License

Repo tags bare versions (`0.1.0`, `0.2.0`), notes on GitHub release, universal binary via `.github/workflows/release.yml`. Beta adds root `CHANGELOG.md`: notes predate tag, survive outside GitHub.

## 1. Repository shape

- [x] 1.1 Root: `README.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `LICENSE`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `AGENTS.md`, `CLAUDE.md`, `Package.swift`, `Package.resolved`, `Makefile`, dot files, `Distribution/`, `Examples/`, `Registry/`, `Scripts/`, `Skills/`, `Sources/`, `Tests/`, `Website/`, `docs/`. `git ls-files` depth 1 after `ef7e727` matches (documenter's report, 22:20); untracked tooling dirs gitignored
- [x] 1.2 Planning/research/log/note files moved to `~/Projects/swiftui-cn-local/`, one-line index, judged by reading: `HANDOFF.md`, `PRODUCT.md`, `GENERAL_DIRECTION_REVIEW.md`, `STAGE_ONE_VALIDATION.md`, `docs/component-roadmap.md`, `docs/research.md`, `docs/research-stage-7.md`, `docs/clean-room-trial.md`, `docs/cli-migration.md`, `tasks/`, untracked `tmp/`, `swarmforge/`. `docs/mango.md` kept as template, citations now plain statements. `ef7e727` removed ten tracked files (2011 lines); `~/Projects/swiftui-cn-local/INDEX.md` lists each with date
- [x] 1.3 Every link/README/skill/test/generator pointing at a moved file updated; `git grep` per name returns nothing outside `docs/catalog/`, `Website/content/`. Commits `3eb4817`, `b0a13a9`, `e6fdf93` fixed every kept file; last two hits (`Registry/items/sheet.json`, `SwiftUIRegistryShowcaseApp.swift`) reworded 22:08, generators reran (`git grep component-roadmap` outside generated paths, checklist: only contract test's comment noting the file left)
- [x] 1.4 `AGENTS.md` describes shipped repository; document map names only existing files; state lives in `CHANGELOG.md`, not a roadmap; verification list runs as written. Commit `e6fdf93` names `CHANGELOG.md` sole state home, archives outside repository; stale CI sentence, floors gone
- [x] 1.5 Every kept markdown file read in full, corrected: stale counts, dead paths, wrong commands, plans as behavior; no em dashes in prose. Commits `3eb4817`, `b0a13a9`, `1be71d7`; `git grep -n "—" -- '*.md' '*.swift' '*.ts' '*.tsx'` returns only `Website/AGENTS.md:4` (`BEGIN/END:nextjs-agent-rules` block), this checklist's own pattern
- [x] 1.6 `CONTRIBUTING.md` opens with layout, verification list; `SECURITY.md` names supported versions by tag; `CODE_OF_CONDUCT.md` unchanged. Commit `1be71d7`; SECURITY names newest tag, `main`
- [x] 1.7 `.github/` keeps issue templates, PR template, Dependabot, CODEOWNERS, three workflows, matching repository after moves. Documenter read workflows/templates, no moved-file reference (report, 22:20); orchestrator widened `ci.yml`'s drift check to item-tokens generator, output (already covered by AGENTS.md list)

## 2. The Stage 9 surface, polished

iOS 27 simulators (iPhone 17, iPad Pro 13-inch), Showcase, both host apps. Failing test first where possible, then fix. Tool chrome MUST stay system values, never tuned theme.

- [x] 2.1 Tune opens card, Select picks innermost item, knob moves it live, chrome stays still. Showcase, iPhone 17: Picked Sign in (`tuning.scope.item`=button, 7 tokens); ring drew tool-blue, card held still (`select-1.png`, argent captures at 22:53); seeFood 1 passed on iPhone 17 Pro QA, padding knob moved card (workflow report)
- [x] 2.2 Drag card off every edge/back; strip stays reachable; card MUST NOT pass strip past safe area. Argent drags 22:56-23:00: leading kept strip/chevron; trailing/bottom held (strip above tab bar); top stopped at status-bar edge (fix `1dd311f`); `PanelGeometryTests` holds clamp; seeFood 2 passed
- [x] 2.3 Resize min/max: usable at min, grip reachable at max. Shrank to 300x260, title dropped, four controls fit; grew full width, grip on home-indicator line (23:02, 23:03); `Resizing stops at the far edges` (`PanelGeometryTests`); seeFood 3 passed
- [x] 2.4 Rotate, card up: frame clamps into new area, remembered per size class. `testFloatingPanelStaysReachableAfterRotation` passed iPad Pro 13-inch (`ipad-tests-1.log`); seeFood 4 passed iPhone, re-clamped landscape; Showcase portrait-only iPhone, skipped there
- [x] 2.5 Collapse/reopen: frame, selection survive. `testCollapsingAndReopeningThePanelKeepsTheSelection` passed iPad (`ipad-tests-1.log`) after collapse-frame fix; seeFood 5 passed
- [x] 2.6 Sheet/full-screen cover under panel: card stays above, sheet's touches work. iPad dialog recipe: sheet opened under column, card above, panel up; Name field took keyboard once app kept key window (1788896326170), Import editor took it back (1788896365020); seeFood first-run Welcome sheet showed Tune above it (kutayib report). Tagged item in sheet not driven, Showcase sheets hold none; seeFood 6 not reached [unverified]; works via environment-propagated reporter
- [x] 2.7 Keyboard in panel field: card keeps place, field reachable, dismisses cleanly. Auth-form's Email field took keyboard, card up (23:19); Import editor took it back (22:14); full suite's import test types into that editor
- [x] 2.8 Select on nested items: chain lists every item under pick, innermost first; empty pick clears selection, disarms Select, no crash. Sign-in-inside-card-inside-form resolved to button, chain button/card/auth-form (On this screen: all three); `testAPickOnNothingClearsTheSelectionAndDisarmsSelect` passed iPhone/iPad; seeFood 8 passed, innermost-first chain in Picked
- [x] 2.9 Reduce Motion: no animated card movement or selection-ring beyond setting. On (`defaults write com.apple.Accessibility ReduceMotionEnabled true`): card landed within 60ms, no crash (23:11); `ToolChrome.animation(reduceMotion:)` returns nil (`The tool moves without animation under Reduce Motion`); two remaining `.snappy` toggle bindings now gated (`1dd311f`); seeFood 9 passed
- [x] 2.10 Dynamic Type at accessibility sizes: rows/drag bar/grip stay usable, minimum grows with text. Accessibility-extra-large: usable, toolbar overflowed natively (`ax-size-1.png`); minimum grows to 360pt, re-clamps (`1dd311f`, `The minimum grows at an accessibility text size`); seeFood 10 passed
- [x] 2.11 Dark/light: card, ring, outlines, guides read on both. Dark: card, dashed ring on both buttons, labels read on dark ground (23:05 capture); light held throughout drive; seeFood verified dark (app pins scheme)
- [x] 2.12 Right to left: default place, snap edge, strip, outlines mirror correctly. `-right-to-left` launch: outlines/pick landed on mirrored items (`rtl-outlines.png`); overlay positions every layer in window points, so system-language RTL gets same result; neither host renders RTL without Arabic localization, system-RTL run unverified [unverified]
- [x] 2.13 VoiceOver: every control has label (Tune, drag bar, collapse, grip, Select capture, On this screen rows). `testEveryPanelControlSpeaksItsLabel` passed iPhone/iPad; argent's describe lists label on every tool control, Showcase and seeFood (check 13)
- [x] 2.14 Host with no page for picked item: panel stays on form, no push, no empty page. No-host-page pick leaves panel on form; `TuningPanel` pushes only when `hostPage` returns view; exercised every pick in Showcase (registers no pages); seeFood 14 not reached [unverified in host with pages]
- [x] 2.15 Host document fails to decode: falls back to shipped document, logs once, MUST NOT crash, file unclobbered until change. Garbage written into Showcase's `registry-tokens.json` (app stopped): relaunch showed defaults, file's SHA unchanged after opening panel (23:13); seeFood 15 passed on its host document; held by two decode tests in `SurfaceBoundaryTests`
- [x] 2.16 Remembered card frame after size-class change: compact/regular stay separate, clamped when restored. Frames under separate keys, restore clamped; `PanelGeometryTests` via `onChange(of: isRegular)` in `FloatingPanel`; seeFood 16 saw regular column iPad; live Split View change not driven [unverified at runtime]
- [x] 2.17 Memory opening/closing panel 50 times: no growth beyond first open (via simulator memory report or XCTest metric). `testOpeningAndClosingThePanelFiftyTimesHoldsMemory` on iPad: deltas per ten cycles 9667, -377, 0, 16, -16 kB (`ipad-tests-1.xcresult`); only first open allocates
- [x] 2.18 Showcase UI suite passes on iPhone 17 pin; two stale references the only known failures (or GOLDEN-CHANGE recapture). 22:38 run (`iphone-full-suite.log`): `Executed 27 tests, with 2 tests skipped and 10 failures`; skips iPad-only; three failures visual references: `auth-light` 2.70%, `nutrition-light` 1.54% (stale refs), `activity-light` 4.55% (text size at medium); seven panel failures (card left small at bottom, pre-layout-reset). Reruns 10/1 at 23:07 (`iphone-rerun.log`), 4/0 at 23:43 after scroll fix (`iphone-rerun-4.log`), activity passed; every test passes on final build except the two stale references
- [x] 2.19 seeFood's Design mode runs on registry product, no app-side workaround; adoption edits noted. Built against tonight's registry via path override; every check reached passed (1-5, 8-11, 13, 15, 16, 18); no app edit beyond override; override restored before morning, pin move to 0.3.0 is owner's step
- [x] 2.20 kutayib builds against registry, installed items render; adoption edits noted. Built with override plus adoption (`import SwiftUIRegistryDesignSurface`, `.designSurface()`); six items render (check 18), every control labeled (check 13); Select finds nothing, items predate tag (CHANGELOG known limitation)

## 3. Edge cases at the product boundary

Every public symbol in `SwiftUIRegistryFoundations`, `SwiftUIRegistryDesignSurface` read: what can a host pass that breaks it.

- [x] 3.1 `ItemSelection.chain`/`pick` with empty frame list, duplicate names, zero-area frames, point outside every frame. `SurfaceBoundaryTests` (empty list, point outside, duplicate names, zero-area frame, equal-area tie order): 43 Showcase package tests passed (`showcase-package-tests-3.log`); ties resolve by name (`ItemSelection.chain`, `bd4b385`)
- [x] 3.2 `designSurface` applied twice in one tree, or `knobs` registered twice for one item: later registration wins, change rejected; MUST NOT crash. `KnobStore.register` merges, later wins (`Registering knobs twice for one item keeps the later registration`, `bd4b385`); second `designSurface` reuses one window (`install()` guards existing window); last `onAppear` writes host hooks
- [x] 3.3 `itemTitle` returning empty string: row/ring fall back to name. `DesignSurfaceState.title` falls back to name for empty host title (`A host title that is empty falls back to the item's name`, `1dd311f`); ring reads same title via `selectedTitle`
- [x] 3.4 `page` for off-screen item, expensive page closure: called once per pick, not per frame. `TuningPanel` calls `hostPage` only on chain change: once per pick, per chain name, until one has page; once more for pushed destination (`TuningPanel.swift`, `onChange(of: chain)`). Off-screen item's page just pushes, no frame read
- [x] 3.5 `registryScreen` names that nest/repeat/are empty; `registryItem` with empty name. `registryItem("")`/`registryScreen("")` report nothing (`RegistryItem.swift`, `bd4b385`); nested/repeated screen names stack, leave by last index (`DesignSurfaceState.screenLeft`)
- [x] 3.6 Token file from newer version, empty, or unknown keys: decode falls back, logs, shipped document applies. `TokenStore` keeps shipped document, logs once via `SurfaceLog`, leaves file untouched (two decode tests, `SurfaceBoundaryTests`, real filesystem via `withDependencies`, `bd4b385`); unknown keys decode fine via synthesized Codable
- [x] 3.7 `ItemKnob` with inverted range, zero step, shipped value outside range: registration clamps or rejects with clear message. `ItemKnob.init` sets non-positive step to 1, clamps shipped value into range, logs it; inverted `ClosedRange` traps in host code first (`A knob with a zero step or a shipped value off its range is made usable`, `bd4b385`)
- [x] 3.8 `TokenDocument.pages` empty, page with no knobs, choice knob's value off its options: no-pages document shows only reset; no-knobs page is empty section; off-options value added to picker (`TokenPageView`, `bd4b385`)
- [x] 3.9 Package tests hold each boundary case UIKit-free; Showcase package tests hold rest, product compiles empty on macOS. `SurfaceBoundaryTests` (11), `PanelGeometryTests` (8); root package holds foundations theme tests (8), RegistryKit (71)

## 4. The tool and the installer

- [x] 4.1 `search`, `describe`, `info`, `install --plan`, `install`, `install --diff`, `install --update`, `preset`: run against scratch package, expected output. Toolsmith's report (22:25): search/describe/plan/install/info; --diff (0, then 1 post-edit); refused reinstall (exit 2); --update; five preset commands; `mcp` tools/list (7 tools); all expected
- [x] 4.2 Same against seeFood's/kutayib's trees; receipts round-trip via `info`, `--diff` reports owned edits, `--update` without `--force` MUST NOT overwrite customized owned item. Toolsmith's report: seeFood 15 items (bubble/button/card/empty owned), kutayib 6 (button/input owned); `--update` left seeFood's sources untouched; kutayib merged input cleanly, stopped on button (exit 2), `.merge` left, owned file byte-identical
- [x] 4.3 Skills' `.swiftinterface` references regenerated from built products, match public API. Commit `8af740e` regenerated foundations `.swiftinterface` after `bd4b385` removed anchor preference
- [x] 4.4 Root package tests pass, count read from log. Toolsmith's `swift test`: RegistryKitTests 71 passed, SwiftUIRegistryFoundationsTests 8 passed, 0 issues (report, 22:25)
- [x] 4.5 Showcase package tests pass, count read from log. `Test run with 44 tests in 5 suites passed` on iPad (`ipad-tests-1.log`, `ipad-tests-2.log`), iPhone (`iphone-full-suite.log`)
- [x] 4.6 Showcase UI suite passes on iPhone 17 pin (see 2.18). 27 full run, then 10/4 reruns on final build, two stale refs only remaining failures; iPad ran 7/9 panel tests, 0 failures (`ipad-tests-1.log`, `ipad-tests-2.log`)
- [x] 4.7 Generated outputs byte-identical after four generators (`git diff --exit-code` on generated paths). Toolsmith's report, exit 0 (`5fa2ada`)
- [x] 4.8 `make format-check` passes: exit 0 (toolsmith's report)
- [x] 4.9 Website typechecks, builds: `npm ci`, `npm run typecheck`, `npm run build` in Website, exit 0 (toolsmith's report)

## 5. Release artifacts

- [x] 5.1 `CHANGELOG.md` root carries beta entry, adopter language: design surface as window over host app, `TokenDocument`, per-item knobs, host hooks, `ItemSelection.chain`, fixed tool chrome, off-edge card, known limitations. Commits `cf15db2` (draft), `91814b2` (finished); Added/Changed/Fixed/Known limitations for 0.3.0; 0.2.0/0.1.0 history from release notes
- [x] 5.2 `README.md` reflects current API, opens with five-minute quickstart, works followed literally. Commit `91814b2`; quickstart snippets quoted from `describe button`, `install --plan` output at 23:08; versions read 0.3.0; design surface section documents three forms, host overload against `DesignSurface.swift`
- [x] 5.3 Four things MUST agree on beta version: version constant `Sources/RegistryKit/ReleaseSnapshot.swift`, Homebrew formula template, every installable item's foundations floor, installer's printed requirement. Commit `5fa2ada`: version constant, formula, 48 item floors, printed requirement agree (`from 0.3.0 up to the next minor version`); update notice no longer advertises older tap tag
- [x] 5.4 Tag/push commands prepared in morning report; nothing pushed or tagged tonight. `git status` shows no push, `git tag` lists only 0.1.0, 0.2.0 at 23:25
- [x] 5.5 Each commit tonight has subject, body (what changed, why), one `By <role>.` line, nothing else. `git log --format=%B ec546e9..HEAD` audited; every commit has subject, body, exactly one `By <role>.` line, no other trailer

## Beta version

`0.3.0`. Foundations gained public API after `0.2.0`: `registryItem`, `registryScreen`, `registryKnob`, reporter. Design surface became second product, minor bump under 1.0. Every installable item applies `registryItem`, so item floor is tag itself.
